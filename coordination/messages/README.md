# Messages

Append-only inter-agent/session messages live here as one JSON file per message.

Schema and routing rules: `../../AGENT_MESSAGE_BUS.md`.

Do not edit or delete another participant's message merely to mark it read; create an acknowledgment under `coordination/acks/` instead.
