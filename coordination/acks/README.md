# Message acknowledgments

Acknowledgments live under:

`coordination/acks/<message-id>/<agent-or-session-id>.json`

Each participant owns only its own acknowledgment file. See `../../AGENT_MESSAGE_BUS.md`.
